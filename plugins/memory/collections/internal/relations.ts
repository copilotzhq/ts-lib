/**
 * Shared relation names between the Memory collection owners.
 *
 * @module
 */
export const MEMORY_EDGE = Object.freeze({
  usesSpace: "uses_memory_space",
  hasRecord: "has_memory_record",
  includesRecord: "includes_memory_record",
});
