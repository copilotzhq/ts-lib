## What it is

The Action that reads the current time.

## Why it exists

Agents need a portable clock.

## How to use it

Compose it through the built-in tools plugin.

## How it works

It reads the injected clock and formats the result for the requested format and
timezone.
